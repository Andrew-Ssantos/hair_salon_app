import 'package:flutter/material.dart';

class HsClientModal {
  Future<void> _showClientModel(BuildContext context) {
    return showDialog(
        context: context,
        builder: (context) {
          return AlertDialog(
              // content: ,
              );
        });
  }
}
